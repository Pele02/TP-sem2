#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
	int n, rez, put, rest;
	printf("Dati numarul dorit: ");
	scanf("%d", &n);
	while (n != 0)
	{
		rest = n % 2;
		rez = rez + rest * put;
		nr = nr / 2;
	}

	return 0;
}