#include<stdlib.h>
#include<stdio.h>

/*int main()
{
	FILE *f;
	int n, v[255], i=0;
	f = fopen("input.txt", "rt");
	fscanf(f, "%d", &n);
	while (!feof(f))
		fscanf(f, "%d", &v[i++]);
	fclose(f);
	
	return 0;
}*/

int rec(int v[], int n, int i)
{
	int j;
	if (i > n)
		return;
	{
		for (j = i + 1; j <= n; j++)
			if (a[i] = a[j]);

	}
}

int main()
{
	int n, i, v[255];
	for (i = 0; i < n; i++)
	{
		printf("v[%d]=", i);
		scanf("%d", &v[i]);
	}	
		
	
}

